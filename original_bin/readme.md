# original_bin README

This is the folder where you put the original binary files.
Both `.tar` and `.grf` are supported.
After excuting `auto_generate.sh` you should be able to find the decoded sprites and codes in `sprites/`.